# dotfiles

These are my dotfiles. Feel free to use as appropriate.

## Notes

`nix-flakey` is a separate directory with a symlink within `home`. That's
because nix really wants [shallow
symlinking](https://github.com/andsens/homeshick/wiki/Symlinking#shallow-symlinking).
