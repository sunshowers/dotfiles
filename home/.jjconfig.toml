[user]
name = "Rain"
email = "rain@sunshowers.io"

[ui]
default-command = "sl"
diff.tool = ["difft", "--color=always", "$left", "$right"]

[aliases]
sl = ["log", "-r", "open()"]
slog = ["log", "-r", "open()", "-T", "builtin_log_detailed"]
sq = ["squash"]

[revsets]
# log = 'open()'

[git]
auto-local-branch = false

[revset-aliases]
# We're uninterested in remote branches from origin etc, and their ancestors. But we *are*
# interested in @git remote branches.
#
# What if a commit is present in both @git and a remote branch? In that case we're interested in the
# commit.
'git_branches()' = 'remote_branches(remote=exact:"git")'
'uninterested()' = '::(remote_branches() ~ git_branches())'
'mine()' = 'author("rain@") | author("rain1@")'
'interested_heads()' = 'mine() ~ uninterested()'
'open()' = '''
    ancestors(interested_heads(), 2)
      | (immutable_heads() ~ tags())
      | ancestors(@, 2)
'''

[templates]
log = 'builtin_log_comfortable'
